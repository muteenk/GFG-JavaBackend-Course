spring:
  application:
    name: splitwise

  datasource:
    url: jdbc:mysql://localhost:3306/splitwise
    username: root
    password: blackhat

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

  cache:
    type: redis

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 9000
  servlet:
    context-path: /api

jwt:
  secret-key: ljsldkfjsldkfjweuroiejf-lsdjflkjieuowu-234234#$#%$#
  access-expiry: 600
