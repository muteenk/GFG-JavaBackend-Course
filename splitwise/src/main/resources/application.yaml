spring:
  application:
    name: splitwise

  datasource:
    url: jdbc:mysql://localhost:3306/splitwise
    username: root
    password: blackhat

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
server:
  port: 9000
  servlet:
    context-path: /api
