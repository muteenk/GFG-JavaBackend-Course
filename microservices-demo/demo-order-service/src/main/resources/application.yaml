spring:
  application:
    name: demo-order-service

  datasource:
    url: jdbc:mysql://localhost:3306/demo-order
    username: root
    password: blackhat

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

server:
  port: 8083
