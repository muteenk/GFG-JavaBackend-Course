spring:
  application:
    name: management
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/gfg_practice
    username: root
    password: blackhat

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

server:
  port: 9000